import { announcement } from './announcement';
import { localeString } from './localeString';
import { quickGoal } from './quickGoal';
import { review } from './review';

export const schemaTypes = [localeString, quickGoal, announcement, review];
